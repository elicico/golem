/* --------- initial state ------------------------------------------- */

const initialState = {
  navItems: {
    counter: '/',
    foo: '/foo',
  },
};

/* --------- reducer ------------------------------------------- */

export default function update(state = initialState) {
  return state;
}
